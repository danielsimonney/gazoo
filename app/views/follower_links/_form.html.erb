<%= form_with(model: follower_link, local: true) do |form| %>
  <% if follower_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(follower_link.errors.count, "error") %> prohibited this follower_link from being saved:</h2>

      <ul>
      <% follower_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :follower_id %>
    <%= form.number_field :follower_id %>
  </div>

  <div class="field">
    <%= form.label :followee_id %>
    <%= form.number_field :followee_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
